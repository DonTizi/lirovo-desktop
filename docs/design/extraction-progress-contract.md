# Extraction progress: editorial status, not a checklist

Preserve the approved native type, translucent shell and palette. Replace raw stage names and repeated status icons with a focused animated status story during work and a quiet processing summary afterward.

```text
engine events + saved attempts -> truthful presentation model
                                  | live: shimmer + four milestones
                                  | terminal: compact summary
                                  + disclosure: complete stage record/errors
```

Pattern: event-driven presentation adapter with progressive disclosure. Four readable chapters group the eight real stages; parallel work, skipped/degraded work and missing records stay explicit. Motion communicates activity, never invented reasoning, fabricated percentages, facts or elapsed estimates.

Acceptance: restrained text shimmer, soft entry transitions, compact numbered milestone rail, useful stage descriptions; terminal summary without a panel of green checks; full diagnostic messages retained; live and recorded state agree; failure/cancel/stopped never shimmer; missing successful-run stage says not recorded, not waiting; reduced motion/forced colors and Pause animations work. Existing graph, font, sidebar, source input, pipeline and recorded data remain unchanged.

Files: RunProgress, new pure progress model/tests, its callers in App and run-view, scoped globals.css, isolated preview/spike and QA notes. No new dependencies, paid extraction, public route, commit or deployment.

spike: yes — read actual SQLite attempts (7 done records, ingest absent), invoke model before tests, and exercise the production component using a clearly labeled isolated replay. Native existing completion path plus preview states prove UI behavior without invoking a paid pipeline.

Verification: direct real-data model; desktop tests/typecheck/build/preload guard; formatting/diff review; independent fresh reviewer; native completed view and isolated live/failure/cancel/reduced-motion responsive smoke. Oracle proves presentation/state transitions, not a new full extraction, output accuracy, all assistive technology or provider behavior. Rollback only this contract's files/hunks; preserve all earlier user changes.

Recall: existing Lirovo graph lesson establishes bounded motion and live preference subscription; continuous ambient motion distinguishes activity from scroll-only effects. No prior polished extraction-progress pattern found. Official CSS text clipping/reduced-motion and WAI status-message guidance cross-checked. New pattern will be captured after proof.
